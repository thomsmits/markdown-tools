::Vararg-Methoden (Multiple-Choice)::[html]<p>Welche der folgenden Code-Fragmente definiert eine Ruby-Methode <code>sum</code>, die eine beliebige Anzahl von Werten nimmt, addiert und die Summe zurückgibt?</p>{=<code>def sum(*p); p.reduce \{ |s, e| s +\= e \}; end</code> ~<code>def sum(**p); p.reduce \{ |s, e| s +\= e \}; end</code> ~<code>def sum(p); p.reduce \{ |s, e| s +\= e \}; end</code> ~<code>def sum(p[]); p.reduce \{ |s, e| s +\= e \}; end</code> ~<code>def sum(p*); p.reduce \{ |s, e| s +\= e \}; end</code> ~<code>def sum(p**); p.reduce \{ |s, e| s +\= e \}; end</code> ~<code>def sum(p...); p.reduce \{ |s, e| s +\= e \}; end</code> }
