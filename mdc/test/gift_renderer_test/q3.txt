::Verwendung des Hammingabstands::[html]<p>Hat ein Code den Hamming-Abstand <code>d</code></p>{ = -> die maximal <code>d - 2</code> Bits betreffen = -> die maximal <code>d</code> Bits betreffen =so können alle Fehler korrigiert werden, die -> die weniger als <code>d/2</code> Bits betreffen =so können alle Fehler erkannt werden, die -> die weniger als <code>d</code> Bits betreffen }
